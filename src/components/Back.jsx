import React from "react";

const Back = ({ styles }) => (
  <button type="button" className={`py-4 px-5 font-poppins font-medium text-[18px] text-primary bg-blue-gradient rounded-[10px] outline-none ${styles}`}>
    Back
  </button>
);

export default Back;
